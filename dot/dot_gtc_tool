## Tools

if [[ "$OSTYPE" == "linux-gnu"* ]]; then
  # GPU programming
  # This is for PyTorch, as the 10.3.5 bin version from AMD
  # has not been released officialy
  export HSA_OVERRIDE_GFX_VERSION=10.3.0
fi

# Git signin
export GPG_TTY=$(tty)

# Tmux
alias tmux="TERM=screen-256color-bce tmux"
